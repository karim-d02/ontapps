/**
 * STUBBED DURING THE SCHEMA MIGRATION — see MIGRATION-REPORT.md.
 *
 * These were the types for the pre-migration data/programs.json. Every shape
 * in here — Program, SuppApp, AverageEntry, TimelineEntry, GatekeepingModel,
 * StaleOfficialPage — describes a file that no longer exists.
 *
 * The current types live in types/schema.ts and cover both data/programs.json
 * and data/gatekeeping.json.
 *
 * Kept as an empty module rather than deleted so a stale import fails at the
 * import site rather than type-checking against a schema that is gone.
 */

export {};
